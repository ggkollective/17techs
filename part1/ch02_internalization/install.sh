#!/bin/bash
# Ubuntu 운영체제에서는 아래 주석을 풀고 gettext 를 설치할 수 있습니다.
# sudo apt-get install gettext

# CentOS는 yum(Fedora 는 dnf) 을 사용합니다.
# yum install gettext

# macOS 는 brew 로 설치할 수 있습니다.
# brew install gettext

# export PATH=$PATH:/usr/local/opt/gettext/bin